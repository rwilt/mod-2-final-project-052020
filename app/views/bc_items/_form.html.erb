<%= form_for @bcitem do |f| %>
     <%= f.label :buyers_cart_id %>
     <%= f.collection_select :buyers_cart_id, @buyerscarts, :id, @bcitem.buyers_cart.buyer.name %>

    <%= f.label :item_id %>
    <%= f.collection_select :item_id, @items, :id, :name %>

    <%= f.submit %>
  
<% end %>