<%= form_for @sellerscart do |f| %>
    <%= f.label :seller_id %>
    <%= f.collection_select :seller_id, @sellers, :id, :name %>

    <%= f.label :item_id %>
    <%= f.collection_select :item_id, @items, :id, :name %>

    <%= f.submit %>

<% end %>