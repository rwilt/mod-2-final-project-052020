<%= form_for @shoppingcartitem do |f| %>
     <%= f.label :shopping_cart_id %>
     <%= f.collection_select :shopping_cart_id, @shoppingcarts, :id, :buyer_id %>

    <%= f.label :item_id %>
    <%= f.collection_select :item_id, @items, :id, :name %>

    <%= f.submit %>
  
<% end %>